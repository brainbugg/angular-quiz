/*IMMEDIATELY INVOKING FUNCTION*/
// Runs automatically when the page loads

(function()
{
	// go find turtlefacts module and return it to me
	angular.module("turtleFacts", [/*dependencies*/]);

})();